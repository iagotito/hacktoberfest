n = int(raw_input())
 
 
for i in range(n):
	m = int(raw_input())
	l = list(raw_input())
 
	for j in range(len(l)):
		if len(l) > 11:
			if l[0] != "8":
				l.pop(0)
				continue
			else:
				l.pop(1)
	if len(l) == 11 and l[0] == "8":
		print "YES"
	else:
		print "NO"
